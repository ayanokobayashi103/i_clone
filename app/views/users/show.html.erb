<h2><%= @user.name %>のページ</h2>
  <% if @user.image.present? %>
    <p><strong>Image:</strong>
    <%= image_tag @user.image.url %></p>
  <% end %>
    <p>メールアドレス: <%= @user.email %></p>
    <% if logged_in? %>
      <% if current_user.id == @user.id %>
          <p><%= link_to '編集', edit_user_path(@user) %></p>
          <p><%= link_to 'お気に入りへ', favorite_path %></p>
      <% end %>
    <% end %>
<p><%= link_to '一覧へ', iclones_path %></p>
